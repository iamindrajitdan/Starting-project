name: Output information
on: push
jobs:
    info:
        runs-on: ubuntu-latest
        steps:
            - name: Output github context
                run: echo "GITHUB_CONTEXT: ${{ toJson(github) }}"